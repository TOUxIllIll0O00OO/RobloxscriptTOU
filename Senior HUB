-- [[ [🔫🌊!]🎐เมืองไทย 2 | UPDATED VERSION ]] --
-- [[ ฟังก์ชันดึงข้อมูลผู้ใช้ (Avatar & Account Info) ]] --

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserId = LocalPlayer.UserId
local thumbType = Enum.ThumbnailType.HeadShot
local thumbSize = Enum.ThumbnailSize.Size150x150

-- ดึงรูปอวาตาร์
local content, isReady = Players:GetUserThumbnailAsync(UserId, thumbType, thumbSize)

-- หน้าหลัก (HomePage) - ล้างของเก่าเพื่ออัปเดตใหม่
for _, child in pairs(HomePage:GetChildren()) do
    if child:IsA("TextLabel") or child:IsA("ImageLabel") then
        child:Destroy()
    end
end

-- สร้างกรอบรูปอวาตาร์ (Avatar Image)
local AvatarFrame = Instance.new("ImageLabel")
AvatarFrame.Name = "UserAvatar"
AvatarFrame.Parent = HomePage
AvatarFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
AvatarFrame.BorderSizePixel = 2
AvatarFrame.BorderColor3 = Color3.fromRGB(255, 0, 0)
AvatarFrame.Position = UDim2.new(0, 0, 0, 5)
AvatarFrame.Size = UDim2.new(0, 80, 0, 80)
AvatarFrame.Image = isReady and content or "rbxassetid://0" -- ถ้าโหลดไม่ทันจะใช้รูปว่าง

local AvatarCorner = Instance.new("UICorner")
AvatarCorner.CornerRadius = UDim.new(1, 0) -- รูปวงกลมเท่ๆ
AvatarCorner.Parent = AvatarFrame

-- ฟังก์ชันสร้างข้อมูลตัวอักษรข้างรูป
local function CreateInfoLabel(text, pos)
    local InfoLabel = Instance.new("TextLabel")
    InfoLabel.Parent = HomePage
    InfoLabel.BackgroundTransparency = 1
    InfoLabel.Position = pos
    InfoLabel.Size = UDim2.new(0.9, 0, 0, 20)
    InfoLabel.Font = Enum.Font.GothamBold
    InfoLabel.Text = text
    InfoLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    InfoLabel.TextSize = 14
    InfoLabel.TextXAlignment = Enum.TextXAlignment.Left
    return InfoLabel
end

-- แสดงข้อมูล ชื่อจริง(Username) / ชื่อเล่น(Display Name) / อายุบัญชี
CreateInfoLabel("👤 ชื่อเล่น: " .. LocalPlayer.DisplayName, UDim2.new(0, 95, 0, 15))
CreateInfoLabel("🆔 ชื่อจริง: " .. LocalPlayer.Name, UDim2.new(0, 95, 0, 35))
CreateInfoLabel("🗓️ อายุบัญชี: " .. LocalPlayer.AccountAge .. " วัน", UDim2.new(0, 95, 0, 55))

-- เส้นคั่นหล่อๆ
local Line = Instance.new("Frame")
Line.Parent = HomePage
Line.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Line.BorderSizePixel = 0
Line.Size = UDim2.new(0.9, 0, 0, 2)

-- แถบเมนูข้าง (Sidebar Container)
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Sidebar.Position = UDim2.new(0, 0, 0, 40)
Sidebar.Size = UDim2.new(0, 120, 1, -40)

local SidebarList = Instance.new("UIListLayout")
SidebarList.Parent = Sidebar
SidebarList.HorizontalAlignment = Enum.HorizontalAlignment.Center
SidebarList.SortOrder = Enum.SortOrder.LayoutOrder
SidebarList.Padding = UDim.new(0, 5)

-- พื้นที่แสดงเนื้อหา (Page Container)
local PageContainer = Instance.new("Frame")
PageContainer.Name = "PageContainer"
PageContainer.Parent = MainFrame
PageContainer.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
PageContainer.BackgroundTransparency = 1
PageContainer.Position = UDim2.new(0, 125, 0, 45)
PageContainer.Size = UDim2.new(1, -130, 1, -50)

local Pages = {}
local TabButtons = {}

-- ฟังก์ชันสร้างหน้าเพจ (Create Page Function)
local function CreatePage(name, icon_text)
    local Page = Instance.new("ScrollingFrame")
    Page.Name = name .. "Page"
    Page.Parent = PageContainer
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.CanvasSize = UDim2.new(0, 0, 2, 0) -- เลื่อนลงได้
    Page.ScrollBarThickness = 2
    Page.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
    
    local PageList = Instance.new("UIListLayout")
    PageList.Parent = Page
    PageList.Padding = UDim.new(0, 8)
    PageList.HorizontalAlignment = Enum.HorizontalAlignment.Center

    -- ปุ่มใน Sidebar
    local TabButton = Instance.new("TextButton")
    TabButton.Name = name .. "Btn"
    TabButton.Parent = Sidebar
    TabButton.Size = UDim2.new(0, 110, 0, 35)
    TabButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    TabButton.Font = Enum.Font.GothamBold
    TabButton.Text = icon_text .. " " .. name
    TabButton.TextColor3 = Color3.fromRGB(150, 150, 150)
    TabButton.TextSize = 12
    TabButton.AutoButtonColor = false

    local TabCorner = Instance.new("UICorner")
    TabCorner.CornerRadius = UDim.new(0, 4)
    TabCorner.Parent = TabButton

    local TabStroke = Instance.new("UIStroke")
    TabStroke.Color = Color3.fromRGB(255, 0, 0)
    TabStroke.Thickness = 1
    TabStroke.Transparency = 1
    TabStroke.Parent = TabButton

    -- ระบบสลับหน้า (Tab Logic)
    TabButton.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        for _, b in pairs(TabButtons) do 
            TweenService:Create(b, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(150, 150, 150), BackgroundColor3 = Color3.fromRGB(30, 30, 30)}):Play()
            b.UIStroke.Transparency = 1
        end
        
        Page.Visible = true
        TweenService:Create(TabButton, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(255, 0, 0), BackgroundColor3 = Color3.fromRGB(40, 0, 0)}):Play()
        TabButton.UIStroke.Transparency = 0
    end)

    Pages[name] = Page
    TabButtons[name] = TabButton
    return Page
end

-- สร้างหน้าตามคำขอ (Home, Farm, Troll, Decor, Settings)
local HomePage = CreatePage("หน้าหลัก", "🏠")
local FarmPage = CreatePage("ฟามเงิน", "💰")
local TrollPage = CreatePage("เกรียน", "🤡")
local DecorPage = CreatePage("ตบแต่ง", "✨")
local SettingsPage = CreatePage("การตั้งค่า", "⚙️")

-- ตั้งค่าหน้าเริ่มต้น
Pages["หน้าหลัก"].Visible = true
TabButtons["หน้าหลัก"].TextColor3 = Color3.fromRGB(255, 0, 0)
TabButtons["หน้าหลัก"].UIStroke.Transparency = 0

-- ฟังก์ชันสร้างองค์ประกอบภายในหน้า (เช่น Label, Button)
local function CreateLabel(parent, text)
    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0.9, 0, 0, 30)
    Label.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    Label.BackgroundTransparency = 0.9
    Label.Font = Enum.Font.GothamMedium
    Label.Text = text
    Label.TextColor3 = Color3.fromRGB(255, 255, 255)
    Label.TextSize = 14
    Label.Parent = parent
    
    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 4)
    Corner.Parent = Label
    return Label
end

-- ตัวอย่างคอนเทนต์ในหน้าหลัก
CreateLabel(HomePage, "ยินดีต้อนรับ: " .. game.Players.LocalPlayer.Name)
CreateLabel(HomePage, "สถานะบอท: พร้อมใช้งาน ✅")
CreateLabel(HomePage, "BY: รุ่นใหญ่ HUB")

-- [[ ส่วนเสริม: ลิงก์ Discord และปุ่มฟังก์ชันหน้าหลัก ]] --

-- ลิงก์ Discord ที่คุณให้มา
local DiscordLink = "https://discord.gg/D83uT6PFgg"

-- เพิ่มปุ่มคัดลอกดิสในหน้าหลักเพื่อความสะดวก
CreateButton(HomePage, "📢 เข้าดิสคอร์ด (Copy Discord)", function()
    setclipboard(DiscordLink)
    Notify("คัดลอกสำเร็จ!", "นำลิงก์ไปวางในเบราว์เซอร์เพื่อเข้าร่วมกลุ่ม 😈")
end)

-- เครดิตด้านล่างหน้าหลัก
local CreditLabel = Instance.new("TextLabel")
CreditLabel.Parent = HomePage
CreditLabel.BackgroundTransparency = 1
CreditLabel.Size = UDim2.new(0.9, 0, 0, 40)
CreditLabel.Font = Enum.Font.LuckiestGuy
CreditLabel.Text = "BY: รุ่นใหญ่ HUB"
CreditLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
CreditLabel.TextSize = 18

-- [[ ระบบการจัดการอื่นๆ ]] --

-- ปรับปรุงหน้าการตั้งค่า (Settings) ให้มีลิงก์ดิสด้วย
CreateButton(SettingsPage, "🔗 Discord: รุ่นใหญ่ HUB", function()
    setclipboard(DiscordLink)
    Notify("Discord Link", "คัดลอกลิงก์แล้ว!")
end)

-- ฟังก์ชันทำลาย GUI เมื่อต้องการเลิกใช้
CreateButton(SettingsPage, "🛑 ทำลายสคริปต์ (Destroy)", function()
    MainGui:Destroy()
end)

-- ตรวจสอบการโหลดซ้ำ (Anti-Double Load)
if CoreGui:FindFirstChild("Studiokidcoolsound_Hub") then
    CoreGui:FindFirstChild("Studiokidcoolsound_Hub"):Destroy()
end
MainGui.Parent = CoreGui

-- สิ้นสุดสคริปต์ [🔫🌊!]🎐เมืองไทย 2
Notify("รุ่นใหญ่ HUB", "ข้อมูลผู้ใช้โหลดสำเร็จ พร้อมออกล่า! 😈🔥")

print("=======================================")
print("  [🔫🌊!]🎐เมืองไทย 2 | LOADED")
print("  USER: " .. LocalPlayer.Name)
print("  DISCORD: " .. DiscordLink)
print("=======================================")
