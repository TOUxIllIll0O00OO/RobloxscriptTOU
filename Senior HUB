-- [[ [🔫🌊!]🎐เมืองไทย 2 | BY: รุ่นใหญ่ HUB ]] --
-- [[ RE-DESIGN: MINI TROLL UI & FIXED SPAM SYSTEM ]] --

local Library = {}
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- ลบของเก่า
if CoreGui:FindFirstChild("Studiokidcoolsound_Hub") then
    CoreGui:FindFirstChild("Studiokidcoolsound_Hub"):Destroy()
end

-- สร้าง ScreenGui
local MainGui = Instance.new("ScreenGui")
MainGui.Name = "Studiokidcoolsound_Hub"
MainGui.Parent = CoreGui
MainGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- ปุ่มเปิด/ปิด (Small Square Top Center)
local ToggleButton = Instance.new("TextButton")
ToggleButton.Parent = MainGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
ToggleButton.Position = UDim2.new(0.5, -15, 0, 5) 
ToggleButton.Size = UDim2.new(0, 30, 0, 30) 
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Text = "X" 
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 0)
ToggleButton.TextSize = 14

local UICorner_T = Instance.new("UICorner")
UICorner_T.CornerRadius = UDim.new(0, 4)
UICorner_T.Parent = ToggleButton

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = MainGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.Position = UDim2.new(0.5, -250, 0.5, -150)
MainFrame.Size = UDim2.new(0, 500, 0, 300)

local UIStroke_M = Instance.new("UIStroke")
UIStroke_M.Color = Color3.fromRGB(255, 0, 0)
UIStroke_M.Thickness = 2
UIStroke_M.Parent = MainFrame

-- หัวข้อ
local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Title.BackgroundTransparency = 0.9
Title.Font = Enum.Font.PermanentMarker
Title.Text = " [🔫🌊!]🎐เมืองไทย 2 | BY: รุ่นใหญ่ HUB "
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 18

-- ระบบเปิด/ปิด
ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
    ToggleButton.Text = MainFrame.Visible and "X" or "O"
end)

-- ลาก GUI
local dragInput, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragStart = input.Position startPos = MainFrame.Position
    end
end)
UIS.InputChanged:Connect(function(input)
    if (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) and dragStart then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- แถบเมนูข้าง (Sidebar)
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Sidebar.Position = UDim2.new(0, 0, 0, 40)
Sidebar.Size = UDim2.new(0, 130, 1, -40)

local SidebarList = Instance.new("UIListLayout")
SidebarList.Parent = Sidebar
SidebarList.HorizontalAlignment = Enum.HorizontalAlignment.Center
SidebarList.SortOrder = Enum.SortOrder.LayoutOrder
SidebarList.Padding = UDim.new(0, 5)

-- พื้นที่แสดงเนื้อหา (Page Container)
local PageContainer = Instance.new("Frame")
PageContainer.Name = "PageContainer"
PageContainer.Parent = MainFrame
PageContainer.BackgroundTransparency = 1
PageContainer.Position = UDim2.new(0, 135, 0, 45)
PageContainer.Size = UDim2.new(1, -140, 1, -50)

local Pages = {}
local TabButtons = {}

-- ฟังก์ชันสร้างหน้าเพจ
local function CreatePage(name, icon_text)
    local Page = Instance.new("ScrollingFrame")
    Page.Name = name .. "Page"
    Page.Parent = PageContainer
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.CanvasSize = UDim2.new(0, 0, 0, 0)
    Page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    Page.ScrollBarThickness = 2
    Page.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
    
    local PageList = Instance.new("UIListLayout")
    PageList.Parent = Page
    PageList.Padding = UDim.new(0, 8)
    PageList.HorizontalAlignment = Enum.HorizontalAlignment.Center

    local TabButton = Instance.new("TextButton")
    TabButton.Name = name .. "Btn"
    TabButton.Parent = Sidebar
    TabButton.Size = UDim2.new(0, 120, 0, 35)
    TabButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    TabButton.Font = Enum.Font.GothamBold
    TabButton.Text = icon_text .. " " .. name
    TabButton.TextColor3 = Color3.fromRGB(150, 150, 150)
    TabButton.TextSize = 12
    TabButton.AutoButtonColor = false

    local TabCorner = Instance.new("UICorner")
    TabCorner.CornerRadius = UDim.new(0, 6)
    TabCorner.Parent = TabButton

    local TabStroke = Instance.new("UIStroke")
    TabStroke.Color = Color3.fromRGB(255, 0, 0)
    TabStroke.Thickness = 1
    TabStroke.Transparency = 1
    TabStroke.Parent = TabButton

    TabButton.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        for _, b in pairs(TabButtons) do 
            TweenService:Create(b, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(150, 150, 150), BackgroundColor3 = Color3.fromRGB(30, 30, 30)}):Play()
            b.UIStroke.Transparency = 1
        end
        Page.Visible = true
        TweenService:Create(TabButton, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(255, 0, 0), BackgroundColor3 = Color3.fromRGB(45, 0, 0)}):Play()
        TabButton.UIStroke.Transparency = 0
    end)

    Pages[name] = Page
    TabButtons[name] = TabButton
    return Page
end

-- สร้างหน้า (Home, Farm, Troll, Decor, Settings)
local HomePage = CreatePage("หน้าหลัก", "🏠")
local FarmPage = CreatePage("ฟามเงิน", "💰")
local TrollPage = CreatePage("เกรียน", "🤡")
local DecorPage = CreatePage("ตบแต่ง", "✨")
local SettingsPage = CreatePage("การตั้งค่า", "⚙️")

-- [[ หน้าเกรียน: ดีไซน์จิ๋วเรียงแนวนอน ]] --
local TrollRow = Instance.new("Frame")
TrollRow.Name = "TrollRow"
TrollRow.Parent = TrollPage
TrollRow.Size = UDim2.new(0.95, 0, 0, 40)
TrollRow.BackgroundTransparency = 1

local TrollRowLayout = Instance.new("UIListLayout")
TrollRowLayout.Parent = TrollRow
TrollRowLayout.FillDirection = Enum.FillDirection.Horizontal
TrollRowLayout.SortOrder = Enum.SortOrder.LayoutOrder
TrollRowLayout.Padding = UDim.new(0, 5)

-- ข้อความ "ทำเซิร์ฟเน่า"
local TrollText = Instance.new("TextLabel")
TrollText.Size = UDim2.new(0, 100, 1, 0)
TrollText.BackgroundTransparency = 1
TrollText.Font = Enum.Font.GothamBold
TrollText.Text = "ทำเซิร์ฟเน่า"
TrollText.TextColor3 = Color3.fromRGB(255, 255, 255)
TrollText.TextSize = 13
TrollText.Parent = TrollRow

-- ช่องใส่จำนวน (เล็ก)
local AmountInput = Instance.new("TextBox")
AmountInput.Size = UDim2.new(0, 80, 1, 0)
AmountInput.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
AmountInput.Font = Enum.Font.GothamBold
AmountInput.PlaceholderText = "[จำนวน]"
AmountInput.Text = ""
AmountInput.TextColor3 = Color3.fromRGB(255, 255, 0)
AmountInput.TextSize = 12
AmountInput.Parent = TrollRow
Instance.new("UICorner", AmountInput).CornerRadius = UDim.new(0, 4)

-- ปุ่มจัดการ (เล็ก)
local ManageBtn = Instance.new("TextButton")
ManageBtn.Size = UDim2.new(0, 80, 1, 0)
ManageBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
ManageBtn.Font = Enum.Font.GothamBold
ManageBtn.Text = "[จัดการ]"
ManageBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ManageBtn.TextSize = 12
ManageBtn.Parent = TrollRow
Instance.new("UICorner", ManageBtn).CornerRadius = UDim.new(0, 4)

-- ระบบสแปมรีโมท (Fixed)
ManageBtn.MouseButton1Click:Connect(function()
    local amt = tonumber(AmountInput.Text)
    if amt then
        local backpack = LocalPlayer:FindFirstChild("Backpack")
        local fireflies = backpack and backpack:FindFirstChild("Fireflies")
        local remote = fireflies and fireflies:FindFirstChild("RemoteEvent")
        
        if remote then
            print("🚀 รุ่นใหญ่ HUB กำลังถล่มเซิร์ฟ: " .. amt .. " ครั้ง")
            for i = 1, amt do
                remote:FireServer()
            end
            print("✅ ถล่มเสร็จสิ้น!")
        else
            warn("❌ ไม่พบไอเทม Fireflies! กรุณาถือไอเทมก่อน")
        end
    end
end)

-- [[ หน้าหลัก VIP: รูปอวาตาร์ & ข้อมูล ]] --
local AvatarImg = Instance.new("ImageLabel")
AvatarImg.Name = "AvatarImg"
AvatarImg.Parent = HomePage
AvatarImg.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
AvatarImg.Size = UDim2.new(0, 80, 0, 80)
AvatarImg.BorderSizePixel = 0
local thumbType = Enum.ThumbnailType.HeadShot
local thumbSize = Enum.ThumbnailSize.Size150x150
local content, isReady = Players:GetUserThumbnailAsync(LocalPlayer.UserId, thumbType, thumbSize)
AvatarImg.Image = isReady and content or "rbxassetid://0"

local AvatarCorner = Instance.new("UICorner")
AvatarCorner.CornerRadius = UDim.new(1, 0)
AvatarCorner.Parent = AvatarImg

local AvatarStroke = Instance.new("UIStroke")
AvatarStroke.Color = Color3.fromRGB(255, 0, 0)
AvatarStroke.Thickness = 2
AvatarStroke.Parent = AvatarImg

local function CreateLabel(text, parent, size)
    local Label = Instance.new("TextLabel")
    Label.BackgroundTransparency = 1
    Label.Size = UDim2.new(1, 0, 0, size or 25)
    Label.Font = Enum.Font.GothamBold
    Label.Text = text
    Label.TextColor3 = Color3.fromRGB(255, 255, 255)
    Label.TextSize = 14
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = parent
    return Label
end

local InfoContainer = Instance.new("Frame")
InfoContainer.Size = UDim2.new(1, 0, 0, 110)
InfoContainer.BackgroundTransparency = 1
InfoContainer.Parent = HomePage
local InfoList = Instance.new("UIListLayout")
InfoList.Parent = InfoContainer
InfoList.Padding = UDim.new(0, 5)

CreateLabel("👤 ชื่อเล่น (Display): " .. LocalPlayer.DisplayName, InfoContainer)
CreateLabel("🆔 ชื่อจริง (User): " .. LocalPlayer.Name, InfoContainer)
CreateLabel("🗓️ อายุบัญชี: " .. LocalPlayer.AccountAge .. " วัน", InfoContainer)
CreateLabel("🚩 สถานะ: ผู้สร้างสรรค์ ⚡", InfoContainer)

-- เพิ่มพื้นที่ว่าง (Padding Space) เพื่อขยับปุ่มลงมา
local Spacer = Instance.new("Frame")
Spacer.Name = "Spacer"
Spacer.Parent = HomePage
Spacer.BackgroundTransparency = 1
Spacer.Size = UDim2.new(1, 0, 0, 15) 

-- [[ ฟังก์ชันปุ่มพื้นฐาน ]] --
local function CreateFunctionalButton(parent, text, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.95, 0, 0, 35)
    Button.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Button.Font = Enum.Font.GothamBold
    Button.Text = text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 13
    Button.Parent = parent
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 6)
    local Stroke = Instance.new("UIStroke", Button)
    Stroke.Color = Color3.fromRGB(255, 0, 0)
    Stroke.Thickness = 1
    Button.MouseButton1Click:Connect(callback)
    return Button
end

-- ลิงก์ Discord ของคุณ
local MyDiscord = "https://discord.gg/D83uT6PFgg"

CreateFunctionalButton(HomePage, "📢 คัดลอกลิงก์ Discord (รุ่นใหญ่ HUB)", function()
    setclipboard(MyDiscord)
end)

CreateFunctionalButton(SettingsPage, "🛑 ปิดใช้งานสคริปต์ (Destroy GUI)", function()
    MainGui:Destroy()
end)

-- ตั้งค่าหน้าแรกให้แสดงผล
Pages["หน้าหลัก"].Visible = true
TabButtons["หน้าหลัก"].TextColor3 = Color3.fromRGB(255, 0, 0)
TabButtons["หน้าหลัก"].UIStroke.Transparency = 0

-- ระบบแจ้งเตือนสุดท้าย
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "[🔫🌊!]🎐เมืองไทย 2",
    Text = "ระบบทำเซิร์ฟเน่าพร้อมใช้งาน! 😈🔥",
    Duration = 5
})

print("---------------------------------------")
print("  [🔫🌊!]🎐เมืองไทย 2 | BY: รุ่นใหญ่ HUB")
print("  STATUS: MINI UI & FIX SPAM")
print("---------------------------------------")
