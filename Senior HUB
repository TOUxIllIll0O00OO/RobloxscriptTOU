-- [[ [🔫🌊!]🎐เมืองไทย 2 | BY: รุ่นใหญ่ HUB ]] --
-- [[ DEVELOPED BY YOUR LUA EXPERT ]] --

local Library = {}
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")

-- สร้าง ScreenGui
local MainGui = Instance.new("ScreenGui")
MainGui.Name = "Studiokidcoolsound_Hub"
MainGui.Parent = CoreGui
MainGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
MainGui.ResetOnSpawn = false

-- ปุ่มเปิด/ปิด (Toggle Button)
local ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "MainToggleButton"
ToggleButton.Parent = MainGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
ToggleButton.BorderSizePixel = 2
ToggleButton.BorderColor3 = Color3.fromRGB(255, 0, 0)
ToggleButton.Position = UDim2.new(0.02, 0, 0.4, 0)
ToggleButton.Size = UDim2.new(0, 100, 0, 40)
ToggleButton.Font = Enum.Font.LuckiestGuy -- ฟอนต์เท่ๆ
ToggleButton.Text = "OPEN/CLOSE"
ToggleButton.TextColor3 = Color3.fromRGB(255, 0, 0)
ToggleButton.TextSize = 14
ToggleButton.AutoButtonColor = true

local UICorner_Toggle = Instance.new("UICorner")
UICorner_Toggle.CornerRadius = UDim.new(0, 8)
UICorner_Toggle.Parent = ToggleButton

-- ตัวหน้าจอหลัก (Main Frame)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = MainGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -250, 0.5, -150)
MainFrame.Size = UDim2.new(0, 500, 0, 300)
MainFrame.ClipsDescendants = true

local UICorner_Main = Instance.new("UICorner")
UICorner_Main.CornerRadius = UDim.new(0, 10)
UICorner_Main.Parent = MainFrame

-- เส้นขอบสีแดงเรืองแสง (Glow Border)
local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(255, 0, 0)
UIStroke.Thickness = 2
UIStroke.Parent = MainFrame

-- หัวข้อ GUI (Title)
local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "Title"
TitleLabel.Parent = MainFrame
TitleLabel.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
TitleLabel.BackgroundTransparency = 0.8
TitleLabel.Size = UDim2.new(1, 0, 0, 40)
TitleLabel.Font = Enum.Font.PermanentMarker -- ฟอนต์แนวสตรีทเท่ๆ
TitleLabel.Text = " [🔫🌊!]🎐เมืองไทย 2 | BY: รุ่นใหญ่ HUB "
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 20
TitleLabel.TextWrapped = true

-- ระบบลาก GUI (Draggable)
local dragInput, dragStart, startPos
local function update(input)
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragInput = nil
            end
        end)
    end
end)
MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)
UIS.InputChanged:Connect(function(input)
    if input == dragInput then
        update(input)
    end
end)

-- ระบบเปิด/ปิด
ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- แถบเมนูข้าง (Sidebar Container)
local Sidebar = Instance.new("Frame")
Sidebar.Name = "Sidebar"
Sidebar.Parent = MainFrame
Sidebar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Sidebar.Position = UDim2.new(0, 0, 0, 40)
Sidebar.Size = UDim2.new(0, 120, 1, -40)

local SidebarList = Instance.new("UIListLayout")
SidebarList.Parent = Sidebar
SidebarList.HorizontalAlignment = Enum.HorizontalAlignment.Center
SidebarList.SortOrder = Enum.SortOrder.LayoutOrder
SidebarList.Padding = UDim.new(0, 5)

-- พื้นที่แสดงเนื้อหา (Page Container)
local PageContainer = Instance.new("Frame")
PageContainer.Name = "PageContainer"
PageContainer.Parent = MainFrame
PageContainer.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
PageContainer.BackgroundTransparency = 1
PageContainer.Position = UDim2.new(0, 125, 0, 45)
PageContainer.Size = UDim2.new(1, -130, 1, -50)

local Pages = {}
local TabButtons = {}

-- ฟังก์ชันสร้างหน้าเพจ (Create Page Function)
local function CreatePage(name, icon_text)
    local Page = Instance.new("ScrollingFrame")
    Page.Name = name .. "Page"
    Page.Parent = PageContainer
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.BackgroundTransparency = 1
    Page.Visible = false
    Page.CanvasSize = UDim2.new(0, 0, 2, 0) -- เลื่อนลงได้
    Page.ScrollBarThickness = 2
    Page.ScrollBarImageColor3 = Color3.fromRGB(255, 0, 0)
    
    local PageList = Instance.new("UIListLayout")
    PageList.Parent = Page
    PageList.Padding = UDim.new(0, 8)
    PageList.HorizontalAlignment = Enum.HorizontalAlignment.Center

    -- ปุ่มใน Sidebar
    local TabButton = Instance.new("TextButton")
    TabButton.Name = name .. "Btn"
    TabButton.Parent = Sidebar
    TabButton.Size = UDim2.new(0, 110, 0, 35)
    TabButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    TabButton.Font = Enum.Font.GothamBold
    TabButton.Text = icon_text .. " " .. name
    TabButton.TextColor3 = Color3.fromRGB(150, 150, 150)
    TabButton.TextSize = 12
    TabButton.AutoButtonColor = false

    local TabCorner = Instance.new("UICorner")
    TabCorner.CornerRadius = UDim.new(0, 4)
    TabCorner.Parent = TabButton

    local TabStroke = Instance.new("UIStroke")
    TabStroke.Color = Color3.fromRGB(255, 0, 0)
    TabStroke.Thickness = 1
    TabStroke.Transparency = 1
    TabStroke.Parent = TabButton

    -- ระบบสลับหน้า (Tab Logic)
    TabButton.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do p.Visible = false end
        for _, b in pairs(TabButtons) do 
            TweenService:Create(b, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(150, 150, 150), BackgroundColor3 = Color3.fromRGB(30, 30, 30)}):Play()
            b.UIStroke.Transparency = 1
        end
        
        Page.Visible = true
        TweenService:Create(TabButton, TweenInfo.new(0.3), {TextColor3 = Color3.fromRGB(255, 0, 0), BackgroundColor3 = Color3.fromRGB(40, 0, 0)}):Play()
        TabButton.UIStroke.Transparency = 0
    end)

    Pages[name] = Page
    TabButtons[name] = TabButton
    return Page
end

-- สร้างหน้าตามคำขอ (Home, Farm, Troll, Decor, Settings)
local HomePage = CreatePage("หน้าหลัก", "🏠")
local FarmPage = CreatePage("ฟามเงิน", "💰")
local TrollPage = CreatePage("เกรียน", "🤡")
local DecorPage = CreatePage("ตบแต่ง", "✨")
local SettingsPage = CreatePage("การตั้งค่า", "⚙️")

-- ตั้งค่าหน้าเริ่มต้น
Pages["หน้าหลัก"].Visible = true
TabButtons["หน้าหลัก"].TextColor3 = Color3.fromRGB(255, 0, 0)
TabButtons["หน้าหลัก"].UIStroke.Transparency = 0

-- ฟังก์ชันสร้างองค์ประกอบภายในหน้า (เช่น Label, Button)
local function CreateLabel(parent, text)
    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0.9, 0, 0, 30)
    Label.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    Label.BackgroundTransparency = 0.9
    Label.Font = Enum.Font.GothamMedium
    Label.Text = text
    Label.TextColor3 = Color3.fromRGB(255, 255, 255)
    Label.TextSize = 14
    Label.Parent = parent
    
    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 4)
    Corner.Parent = Label
    return Label
end

-- ตัวอย่างคอนเทนต์ในหน้าหลัก
CreateLabel(HomePage, "ยินดีต้อนรับ: " .. game.Players.LocalPlayer.Name)
CreateLabel(HomePage, "สถานะบอท: พร้อมใช้งาน ✅")
CreateLabel(HomePage, "BY: รุ่นใหญ่ HUB")

-- [[ ระบบฟังก์ชันเสริมสำหรับ Exploiter ]] --

-- ฟังก์ชันสร้างปุ่มกดภายในหน้าเพจ (Create Function Button)
local function CreateButton(parent, text, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0.9, 0, 0, 35)
    Button.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    Button.Font = Enum.Font.GothamBold
    Button.Text = text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 13
    Button.AutoButtonColor = true
    Button.Parent = parent

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 6)
    Corner.Parent = Button

    local Stroke = Instance.new("UIStroke")
    Stroke.Color = Color3.fromRGB(255, 0, 0)
    Stroke.Thickness = 1
    Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    Stroke.Parent = Button

    Button.MouseButton1Click:Connect(function()
        -- Effect ตอนกด
        Button.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        Button.TextColor3 = Color3.fromRGB(0, 0, 0)
        wait(0.1)
        Button.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
        Button.TextColor3 = Color3.fromRGB(255, 255, 255)
        
        callback()
    end)
    
    return Button
end

-- [[ ตัวอย่างการเพิ่มปุ่มในแต่ละหน้า ]] --

-- หน้าฟามเงิน
CreateButton(FarmPage, "💰 เริ่มฟามอัตโนมัติ (Auto Farm)", function()
    print("Auto Farm Started!")
    -- ใส่โค้ดฟามของคุณตรงนี้
end)

-- หน้าเกรียน (Troll)
CreateButton(TrollPage, "🤡 ขโมยของในแมพ (Steal Items)", function()
    print("Trolling Started!")
end)

CreateButton(TrollPage, "🚀 วาร์ปไปหาคนอื่น (TP to Player)", function()
    print("Teleporting...")
end)

-- หน้าตบแต่ง (Decor)
CreateButton(DecorPage, "🌈 เปลี่ยนสีตัวละคร (Rainbow Skin)", function()
    print("Rainbow Mode On!")
end)

-- หน้าการตั้งค่า (Settings)
CreateButton(SettingsPage, "❌ ปิดสคริปต์ทั้งหมด (Destroy GUI)", function()
    MainGui:Destroy()
end)

CreateButton(SettingsPage, "📋 คัดลอกลิ้งค์ Discord", function()
    setclipboard("https://discord.gg/yourlink")
end)

-- [[ ระบบแจ้งเตือนเมื่อรันสคริปต์สำเร็จ ]] --
local function Notify(title, text)
    local Bindable = Instance.new("BindableFunction")
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = title;
        Text = text;
        Duration = 5;
        Button1 = "ตกลง";
        Callback = Bindable;
    })
end

-- ส่งข้อความปิดท้ายเมื่อโหลดเสร็จ
Notify("รุ่นใหญ่ HUB", "สคริปต์พร้อมใช้งานแล้วครับนายท่าน! 😈")

-- บรรทัดสุดท้าย: แสดงเครดิตใน Console ตามคำสั่งเดิม
print("---------------------------------------")
print("BY: รุ่นใหญ่ HUB")
print("Loaded: [🔫🌊!]🎐เมืองไทย 2")
print("---------------------------------------")
